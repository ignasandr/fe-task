import styles from './Example.module.scss';

export default function Example() {
  return (
    <section className={styles.section}>
      <h1 className={styles.try}>Hello</h1>
      <p>World</p>
      <h2>REFER FRIENDS AND GET REWARDS</h2>
    </section>
  )
}